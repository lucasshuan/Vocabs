<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Memo de voz. É a única permissão sensível do app: a foto usa o app de
         câmera do sistema, que não exige permissão nossa. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Nem todo aparelho tem câmera (e nenhum é obrigado a ter para o app
         funcionar): a captura por foto simplesmente não aparece sem ela. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- A partir do Android 11, o motor de TTS e o app de câmera precisam ser
         declarados visíveis para conseguirmos resolver os intents deles. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- Temporary, for the schema rewrite: auto-backup can restore a pre-rewrite
         database over a fresh install, and the crash that follows reads like a
         code bug. Once the schema settles this is an open product question again
         (no account means backup is the only thing standing between a lost phone
         and lost cards) — revisit rather than assume it must stay false. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vocabs">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
